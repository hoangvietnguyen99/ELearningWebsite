<section class="bg-lighter">
    <div class="container pb-60">
        <table class="table table-dark table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Author</th>
                    <th>Price</th>
                    <th>#</th>
                </tr>
            </thead>
            <tbody>
                {{#each cart.courses}}
                <tr>
                    <td scope="row"></td>
                    <td>{{course.name}}</td>
                    <td>{{course.author}}</td>
                    <td>{{course.price}}</td>
                    <td>
                        <i class="fa fa-minus-circle" aria-hidden="true" style="font-size: 2rem"></i>
                    </td>
                </tr>
                {{/each}}
                <tr>
                    <td scope="row"></td>
                    <td>{{course.name}}</td>
                    <td>{{course.author}}</td>
                    <td>{{course.price}}</td>
                    <td>
                        <i class="fa fa-minus-circle" aria-hidden="true" style="font-size: 35px; color: #F2184F"></i>
                    </td>
                </tr>
            </tbody>
            <tfoot>

            </tfoot>
        </table>
    </div>
</section>